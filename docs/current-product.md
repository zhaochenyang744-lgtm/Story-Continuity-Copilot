# Story Continuity Copilot：当前产品与验证范围

本页是当前产品说明入口。历史阶段记录保留原结论；历史文档里的“当前”“待验收”只描述当时状态。

## 版本与运行状态

- 当前线上版本：v1.4.0 维护更新，发布 ID `maint-e2d141c23d0c-20260913`，2026-09-13（Australia/Sydney）部署。见 [维护部署记录](maintenance-deployment.md)；[9 月 8 日记录](v1.4.0-deployment.md)保留当时范围。
- 维护更新包含作品导出、作者判断复用、历史章节修订与事实复核、日常维护工具。
- 生产 schema 已从 145 升至 146；隔离迁移与切换后比较保留 67 张既有表、661 行数据，完整性及外键检查通过。新回滚预检拒绝不支持历史修订契约的目标镜像，未实际回滚。
- 本地完整验收后已完成生产切换及 7 项公开浏览器检查。线上检查覆盖导出、维护正文、拒绝写入的修订守卫、移动阅读和资源加载；没有线上重跑成功修订、跨 Run 判断复用或 Provider 流程。
- 三个运维计时器已启用，首跑和后续监测周期已观察；手动工作站副本已校验并完成隔离 SQLite 恢复写入验证。用户已明确暂缓自动实例外复制与外部通知，保留为可选后续项；缺少自动副本的告警继续保留。本页为冻结发布包之后的文档更新，不改变归档或镜像身份。
- 真实作者研究尚无完成证据，本轮没有把自动测试、AI 模拟或作者定义的测试文本记为真实用户验证。

## 作者可以完成什么

作品的主流程是：建立或导入作品 → 维护未来规划 → 保存正文 → 检查连续性 → 阅读依据 → 作出作者决定 → 审阅并提交事实变化。

v1.4.0 已包含作品与账号隔离、教学体验、规划与已写材料对照、人物别名、作品问答、变更影响分析、伏笔候选、修订建议与任务、富文本和沉浸写作、当前设备草稿恢复、可追溯的来源与事实版本。

维护更新新增以下入口：

| 能力 | 入口与结果 | 边界 |
|---|---|---|
| 作品导出 | 项目概览下载 TXT、Markdown 或完整 ZIP 资料包 | 默认导出已入库正文；可另外包含当前已保存草稿，浏览器未保存文字不进入导出 |
| 判断复用 | 项目概览进入“修订历史章节与复核事实”，对已有明确作者决定启用或停止复用 | 仅对正文、证据及相关资料均未改变的匹配问题生效；原问题与证据仍可查 |
| 历史章节修订 | 选择具有完整正文的已入库章节，编辑、预览影响，再明确提交 | 先完成现有资料初始化或增量确认；新正文产生新来源版本，旧正文与审计保留；并发更新拒绝静默覆盖 |
| 事实复核 | 逐项阅读修订后的来源，选择保留或停止沿用受影响的事实 | 保留必须绑定新来源；待复核期间阻止把旧事实继续用于新 AI 判断；AI 不代替作者确认 |
| 日常维护 | 独立维护 CLI、调度安装包、结构化状态与运行记录 | 安装、启用和真实异地副本分别核验；本地镜像测试不表示异地备份已运行 |

桌面提供写作和编辑；窄屏保留阅读范围。导出是读取已保存内容，不改变作品。未提交的历史章节修订按账号和作品保留在当前设备，返回后可以明确恢复；恢复不自动覆盖服务器正文，章节版本冲突仍需重新核对。浏览器存储不可用时会提示先完成提交再离开。

## 数据与作者控制

未来规划、已写正文、作者确认事实、AI 候选始终分层。AI 输出不能直接写入正文或事实库。事实提交、历史章节修订和作者判断均绑定项目、账号及有效版本，并保留审计。失败、取消、超时和过期输入不产生部分确认结果。

资料导出保留内容层次与来源关联；未确认、失效或需要复核的条目不混入“当前已确认事实”。导出不包含账号密码、会话令牌、邮箱或 Provider 凭据。

部分历史教学样本只保存来源片段，没有完整章节正文。这类章节可读、可导出已有片段，明确标记材料范围；不能把片段当完整原稿修订。作者导入完整作品后可以使用完整章节修订流程。

## 验证应如何理解

- 历史 v1.0 发布验收覆盖其冻结版本的公开主流程、Provider、邮件、隔离、备份和恢复。
- 历史 10 万和 30 万字验证支持当时的容量、性能与流程结论，不直接代表当前新能力在任何长篇小说上的语义准确率。
- v1.4.0 最终短样本真实 AI 验收使用 `deepseek-v4-flash`；生产部署保留 `deepseek-v4-pro`。当时部署没有重复 AI 效果验收。
- 本轮对生产模型配置的验证使用独立本地数据、固定案例、真实请求计数与分项结果；合成语料与作者定义事实是测试夹具，不是实际用户作品研究。
- 模型原始误判、产品规则降级、最终可提交结果分别记录。“产品场景通过”不等于“模型零误判”。费用缺失记录为不可得，不能当作零费用。

2026-09-12 当前生产模型本地固定集回归已完成：第二轮产品安全与引用检查 12/12，人工首次模型语义及引用核对 10/12；知识变化、简报完整性和目标章节召回仍有已记录限制。分项口径见 [当前模型验证](current-model-acceptance.md)。

本轮检查结果、失败修复记录与分层验收范围见 [维护验收记录](maintenance-acceptance.md)和[维护部署记录](maintenance-deployment.md)。日常运行命令与边界见 [运维说明](operations.md)。

## 尚未覆盖的使用范围

真实作者试用、商业 SLA、多人同时协作、移动端完整写作和通用小说质量评价没有完成证据。作者判断复用是可撤销的明确设置，不是模型训练或无限期忽略规则。完成历史事实复核后，后续章节和增量事实仍需按作品变化显式重新检查。

既有技术契约可查 [v1.3.0 历史契约](v1.3.0-product.md)，历史质量与失败证据可查 [验证与限制](verification-and-limitations.md)。
