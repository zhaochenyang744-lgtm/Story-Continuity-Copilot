# v1.4.0 deployment record

Deployed on 2026-09-08 (Australia/Sydney) to the existing Tencent Lighthouse instance, using the authenticated OrcaTerm session. Public origin: <https://43-160-207-57.sslip.io>.

## Version

- Release: `v140-4bd8d93-20260908`.
- Source: `4bd8d93e3f5dc2bcab916563329f757bb4081639`.
- Accepted runtime source: `8a55f5b3454182736bdd1c0f0dc348a0d09a064e`; the later commit only changes the deployment scanner and tests.
- Frontend build: `s13v4-c23d22feff6a4468ddcf6d66c8f03872`.
- Previous release: `v120-a35265e-20260902`.
- Production provider configuration remains DeepSeek / `deepseek-v4-pro`; server-side credentials and mail configuration were preserved without being exported into the bundle or Git.

## Build and data protection

The deployable bundle was generated from a Git archive, contained 67 files, and excluded local runtime data, tests, control files, dependencies and prebuilt Windows output. Source-bundle content SHA256: `16217D6E0CF4EF9ECB74D90463837AEB98A6413CC189139665BD96A06C631BE5`.

Four exact reviewed PNG hashes contain the public CA email in C2PA certificates. The scanner preserves these certificates and permits that email only for those unchanged files. Mutations, ordinary source emails and known secrets remain rejected. Scanner/deployment regression: 11/11 passed.

An online SQLite backup was downloaded and independently hash-checked before switching. The final pre-switch backup had the same hash, confirming that the off-instance copy covered the cutover state. Neither database contents nor credentials are committed to Git.

A network-isolated container rehearsed migration on the backup copy. All original columns and rows in 41 preexisting business tables matched before and after. The same comparison passed against the live database immediately after migration, before the acceptance visitor was created. Schema advanced from 120 to 145; integrity was `ok`, with zero foreign-key violations.

Backend and frontend were built on Linux. The frontend image passed the existing amd64/musl/native-module verification. Both application containers became healthy and the public readiness endpoint returned `ready`. The first readiness request during the Caddy restart briefly failed to connect; the deployment script retried and exited successfully after recovery.

## Acceptance scope

- Public health/readiness and existing database, provider and mail readiness capabilities.
- Real browser home and projects rendering with loaded images and styles.
- A story plan created through the UI in a new visitor workspace, then found after a full reload.
- Bold text rendered as formatting, saved to the backend and retained after reload, without visible Markdown markers.
- Immersive writing retained the saved text, placed the four common formatting tools beside typography controls, and showed the save state correctly.
- Desktop and narrow-screen route checks are recorded in the local PM2 deployment evidence.

The earlier real-AI short-sample acceptance was performed against DeepSeek / `deepseek-v4-flash`. This deployment preserves the existing production `deepseek-v4-pro` configuration and does not claim to repeat that AI evaluation on the production model. No new AI or SMTP call was made during the deployment smoke test.

## Rollback

The previous release directory, images and configuration remain intact. Deployment state records the previous release as `v120-a35265e-20260902`. Use the retained configuration and images for an application rollback after checking migration compatibility; do not automatically restore an older database over new user activity. A database restore requires a fresh backup and the explicit stopped-application restore procedure in the deployment runbook.
